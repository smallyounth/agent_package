# AI 协作说明

## 使用 AI 做了什么

本次交付中，AI 主要承担三类工作：

1. 扫描项目结构，识别 FastAPI、LangGraph、RAG、Milvus 和 MCP 的真实调用链。
2. 生成面试验证包的文档结构、10 分钟验证路线和关键文件说明。
3. 编写一个可离线验证的 AI 增强模块，并用测试样例验证输出。

## 人类需要把关的内容

以下内容仍应由项目作者或面试者确认：

- 原项目真实业务背景是否完全符合自己的经历。
- DashScope API Key、Milvus、MCP 服务是否能在面试机器上完整启动。
- 录屏时是否展示真实运行环境，而不是只展示离线 demo。
- “本人负责”的文件范围是否和真实项目经历一致。

## AI 增强点为什么合理

原项目已经具备 AIOps Agent 状态流，但诊断结果更偏过程型。面试中常见问题是：面试官没有时间等完整外部依赖启动，也不容易快速看懂 Agent 到底做了什么。

因此本次新增的增强点是“结构化诊断摘要”：

- 输入：AIOps 事件和工具观测结果。
- 输出：风险等级、根因、证据、风险解释、下一步动作。
- 优点：不改变原 Agent，只增加报告后处理层，验证成本低。

## AI 使用边界

- 没有读取 `.env` 或密钥内容。
- 没有推送 GitHub、部署、发布或调用外部账号。
- 没有把 Agent 设计成自动执行重启、扩容、回滚等高风险动作。
- 所有新增可运行代码都放在本目录内。
