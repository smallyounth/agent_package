# AI 协作说明

## 使用 AI 做了什么

本次交付中，AI 主要承担三类工作：

1. 将 AIOps Agent 落地项目中的核心判断链路抽象成一个独立最小验证项目。
2. 生成面试验证包的文档结构、10 分钟验证路线和关键文件说明。
3. 编写可离线运行的 AI 增强模块，并用测试样例验证输出。

## 人类需要把关的内容

以下内容仍应由项目作者或面试者确认：

- README 中的项目经历表述是否完全符合自己的真实经历。
- 录屏时是否清楚展示 `run_full_verification.py` 的运行结果和日志。
- “本人负责”的文件范围是否和真实项目经历一致。

## AI 增强点为什么合理

生产级 AIOps Agent 往往依赖后端服务、模型账号、日志平台和监控平台。面试中常见问题是：面试官没有时间配置这些外部依赖，也不容易快速看懂 Agent 到底做了什么。

因此本次新增的增强点是“结构化诊断摘要”：

- 输入：AIOps 事件和工具观测结果。
- 输出：风险等级、根因、证据、风险解释、下一步动作。
- 优点：把外部依赖替换成样例事件，保留状态流和工具边界，验证成本低。

## AI 使用边界

- 没有读取 `.env` 或密钥内容。
- 没有把 Agent 设计成自动执行重启、扩容、回滚等高风险动作。
- 所有可运行代码都在当前仓库内，面试官 clone 后可直接验证。
